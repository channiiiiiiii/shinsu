# Shisu 구현 기록

## 라이브 스탯 카드 수정

- 메인에서 보석 LV 원형 배지를 제거하고 HP/ATK/DEF/SPD/CRIT 실제 수치를 차트 오른쪽에 배치했다.
- 보석창에는 지정 이름 5종과 장착 레벨을 표시한다. 기기별 이모지 누락 방지를 위해 인라인 SVG 아이콘을 사용한다.
- Edge 320/390/1000px에서 좌우 배치·가로 넘침·실제 수치·장착 Lv 갱신 검증, 화면 캡처 확인, 24개 테스트 및 JavaScript 구문 검사 통과.

## 2026-09-15 / 0.3.3 전투 스탯 UI 개편

- 체력·공격력·방어력·속도·치명타를 SVG 오각형 차트와 지정 색상의 원형 보석 LV 배지로 표현한다.
- 모험과 레이드 상단에 내 레벨·전투력·5대 실제 스탯을 표시하고 콘텐츠 입장 레벨과 함께 비교할 수 있게 한다.
- 모바일에서는 5개 원형 배지를 한 줄로 축소하며 JavaScript 구문과 전체 API 테스트 24개를 통과했다.

## 2026-09-15 / 0.3.2 Lv.1 초기 신수 리롤

- 웹 플레이 시작 자금을 보존하도록 Lv.1에서 계정당 무료 세 번까지 새 알을 부화한다.
- 새 신수의 종족·속성·성격·개체값을 다시 추첨하고 해당 종족 전용 보물 +0을 장착한다.
- 기존 저장에는 `initial_rerolls_used=0`을 안전하게 보완하고 사용 횟수를 즉시 영구 저장한다.
- 무료 3회 성공, 골드 유지, 4회차 및 Lv.2 요청 차단을 자동 테스트로 확인했다.

## 2026-09-15 / 0.3.1 초대 회원가입

- 기존 `player2` 접속코드를 1회용 초대코드로 사용해 한 명만 가입할 수 있도록 구성했다.
- 가입 닉네임과 PBKDF2-SHA256 비밀번호 해시만 SQLite에 저장하며 원문 비밀번호는 저장하지 않는다.
- 가입 완료 후에는 기존 초대코드 로그인을 차단하고 새 비밀번호만 허용한다.
- 잘못된 초대코드·중복 가입·초대코드 재로그인 차단과 재시작 후 비밀번호 로그인을 자동 검증했다.

## 2026-09-15 / 0.3.0 착수

- 접속 코드를 노출하지 않는 계정 이름 API를 추가해 `SHISU_ACCOUNTS.name`을 로그인 선택지에 표시한다.

- 기존 파밍 가이드·도메인·Discord 입장 및 보상 규칙을 대조했다.
- 개인과 협동 전투는 공통 웹 전투 계산을 사용하며, 협동은 두 계정이 입장을 승인한 대기방을 사용한다.
- 모든 참여자의 정산과 방 상태 변경은 같은 SQLite 트랜잭션으로 처리한다.
- 외부 저장소는 서버 전용 백업/복원 도구로 연결하고 기존 운영 저장의 계정 배정은 명시적으로 지정한다.

### 0.3.0 연결 결과

1. 개인·협동 레이드, 동일 보스 HP에 대한 두 신수 공격, 원자적 보상과 패배 저장·중복 정산 방지 구현.
2. 강화·별 각성·잠재·제작·분해·환생·상점 명령과 화면 연결.
3. 성장 스킬·조건부 각인·생활 효과 및 신화·고대 던전 확장. 보스 패턴은 원본 데이터와 선택 함수를 사용한다.
4. 세이브 이전 전 DB 백업·대상 계정 지정·세션 만료, Supabase 서버 전용 자동 백업과 복원 명령 구현.
5. 임시 데이터에서 10종·5난이도 전투 실행, 협동 방 생성·참가·완료 및 재전송 시 같은 저장 결과를 확인했다. 응답 시간 필드만 요청별로 다르다.
6. 수동 플레이·밸런스 및 외부 Supabase 실접속 검증은 미실행이며, 사용자 확인 항목을 docs/EXPANSION.md에 기록했다.

## 2026-09-15 / 신수 이름 변경 연결 착수

- 원본 `Pet.rename`이 공백 이름을 거부하고 15자 제한 및 사용자 지정 이름 상태를 기록하는 것을 확인했다.
- 기존 원자적 행동 API와 신수 화면에 이어 붙여 새 저장 형식을 추가하지 않는다.

### 이름 변경 연결 결과

1. 신수 카드에 이름 변경 버튼·입력·글자 수·저장 및 취소 UI를 추가했다.
2. 원본 `Pet.rename`의 공백 제거, 1~15자 검증, `is_custom_name` 처리를 SQLite 원자적 행동 API에서 호출한다.
3. API 저장 유지와 잘못된 이름 무변경, 실제 Edge 모바일 입력·재로그인 유지 경로를 검증한다.

## 2026-09-15 / 0.2.1 에셋 이식 착수

- DAMAGOCHI 에셋 61개 약 146MB를 확인했다. 모바일 전송량을 줄이기 위해 실제 사용하는 종족 Stage·보스·홍보 이미지만 WebP로 변환한다.
- 원본은 수정하지 않고 `tools/prepare_assets.py`로 변환 과정을 재현 가능하게 관리한다.

### 0.2.1 에셋 이식 결과

1. 종족별 Stage 이미지 40장, 보스 5장, 홍보 4장을 WebP로 변환했다.
2. 원본 약 146MB 전체 대신 실제 사용할 49장을 약 7.9MB로 최적화했다.
3. 로그인 화면에 대표 이미지를 배치하고 신수의 `species_key`와 `stage`에 맞춰 초상화를 자동 교체한다.
4. 이미지가 없거나 로드되지 않으면 기존 종족 이모지를 표시한다.

## 2026-09-15 / 0.2.0 착수

- 현재 비인증 사용자 ID API와 파밍 시제품을 확인했다.
- 두 고정 계정, 서버 세션, SQLite 단일 서버로 구성한다. 저장 트랜잭션 안에서 행동을 계산하고 실패 시 전체 롤백한다.
- 원본 규칙은 별도 legacy 패키지에 출처를 기록하고 웹 서비스가 명시적으로 호출한다.

### 0.2.0 구현·검증 결과

1. 두 고정 계정의 긴 접속 코드와 HttpOnly/SameSite 세션, 운영 Secure 쿠키, 로그인 속도 제한을 구현했다.
2. 모든 게임 변경을 BEGIN IMMEDIATE 트랜잭션에서 계산·저장하며 동일 요청 번호의 재전송을 한 번만 정산한다.
3. 원본 Pet/Inventory/AdventureSystem을 상대 import로 독립 이식하고 돌봄·훈련·던전·아이템·각인·보석 화면을 연결했다.
4. 합성에서 장착 보석 한 개를 예약해 재료 소모 후 보너스만 남는 상태를 방지했다.
5. 실제 Edge 모바일 브라우저에서 로그인 → 던전 → 보석 장착 → 로그아웃 → 두 번째 계정의 빈 가방/초기 골드 확인을 통과했다. 가로 넘침과 페이지 자바스크립트 오류도 검사했다.
6. 테스트에서 SQLite 저장 실패·동시 중복 요청·재시작·계정 격리·백업 복원을 확인했다.
7. 학교 환경 Python 3.10에서도 실행되도록 최소 버전 표기를 조정했다. 배포 및 CI는 Python 3.11 설정이다.
8. Render 영구 디스크 배포 파일과 로컬 접속 코드 자동 생성 스크립트를 작성했다. 유료 서비스 생성은 미실행이다.
9. 기존 파밍 가이드의 ‘전체 적용’ 오해를 정정했다. 조건부 각인·성장 스킬·상위 난이도와 웹 레이드는 미완료로 명시했다.
10. 브라우저 검증 도구와 화면 캡처는 Git 제외된 `.local` 아래에만 저장했다.
11. 최종 자동 테스트 19개 통과, Python 구문 검사·JavaScript 구문 검사·Git 공백 검사 통과. 테스트 의존성의 기존 httpx 사용 중단 예정 경고 1개가 남아 있다.
12. 새로 스테이징한 원본 스냅샷의 줄 끝 공백을 추가 검사에서 발견해 복사본만 정리했다. 원본 규칙과 운영 데이터는 유지했다.
13. Render가 예전 `starter` 명칭을 `0.5c-512mb`로 변경한 공식 문서에 맞춰 Blueprint 플랜 ID와 운영 안내를 갱신했다.

## 2026-09-15 / 0.2.2 신수 이름 변경

1. 신수 이름 옆에 이름 변경 폼을 연결하고, 15자 제한·공백 이름 검증 및 저장 결과를 화면에 반영했다.
2. API 명령으로 기존 도메인 이름 변경 로직을 호출하고 SQLite에 영구 저장되도록 했다. 커스텀 이름은 성장 후에도 유지된다.
3. 두 번째 계정으로 전환 후 첫 계정 재로그인 시 이름 유지와 계정 격리를 브라우저에서 확인했다.
4. API 자동 테스트 21개 통과, 브라우저 스모크 테스트 통과, JavaScript 구문 검사를 통과했다.
5. 정적 파일 URL에 0.2.2 캐시 버전을 붙여 브라우저가 최신 화면을 받도록 했다.

## 2026-09-15 / 0.1.0

1. DAMAGOCHI의 작업 문서와 `DAMAGOCHI_NEW_FARMING_SYSTEM.md`를 먼저 확인했다.
2. 기존 `farming.py`에서 가이드 핵심 규칙이 적용된 것을 코드 기준으로 확인했다.
3. Discord 객체 결합을 제거하고 순수 `PlayerState` 도메인으로 파밍 규칙을 이식했다.
4. API 상태 변경을 사용자별 `asyncio.Lock` 안에서 처리하고 저장 실패 시 스냅샷으로 복원하도록 구성했다.
5. 앱 버전과 저장 버전을 분리하고 독립 Git·CHANGELOG 정책을 추가했다.
6. FastAPI가 동일 출처에서 PWA를 제공해 초기 배포 단위를 하나로 유지하도록 구성했다.
7. 학교 지정 Python으로 전체 구문 검사와 자동 테스트를 수행해 `8 passed`를 확인했다.
8. API 정적 파일 루트 계산 오류를 테스트에서 발견해 `Shisu` 루트 기준으로 수정했다.
9. 독립 Git 저장소와 `main` 브랜치를 초기화하고 GitHub 사용자 `channiiiiiiii`의 비공개 noreply 주소를 이 저장소에만 설정했다.

> 잡지식: 저장 포맷 버전과 앱 버전을 분리하면 배포 롤백과 데이터 마이그레이션을 서로 독립적으로 통제할 수 있어요.
